@extends('layouts.admin')
@section('title','Site Assets')
@section('page-title','Site Assets')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2>Site Assets</h2><p class="muted mb-0">Manage machinery and equipment.</p></div><a class="btn btn-primary" href="{{ route('admin.site-assets.create') }}">Add Asset</a></div>
<form class="card card-body mb-4" method="GET"><div class="row g-2"><div class="col-md-4"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search assets"></div><div class="col-md-3"><select class="form-select" name="site_id"><option value="">All sites</option>@foreach($sites as $site)<option value="{{ $site->id }}" @selected(request('site_id')==$site->id)>{{ $site->site_name }}</option>@endforeach</select></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['available','working','maintenance','breakdown','inactive'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-outline-light w-100">Filter</button></div></div></form>
<div class="card overflow-hidden"><div class="table-responsive"><table class="table table-dark table-hover mb-0"><thead><tr><th>Code</th><th>Asset</th><th>Site</th><th>Category</th><th>Status</th><th></th></tr></thead><tbody>@forelse($assets as $asset)<tr><td>{{ $asset->asset_code }}</td><td>{{ $asset->asset_name }}</td><td>{{ $asset->workSite->site_name ?? '-' }}</td><td>{{ $asset->category }}</td><td>{{ ucfirst($asset->status) }}</td><td class="text-end"><a class="btn btn-sm btn-primary" href="{{ route('admin.site-assets.edit',$asset) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.site-assets.destroy',$asset) }}" onsubmit="return confirm('Delete this asset?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-4">No assets found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $assets->links() }}</div>
@endsection
