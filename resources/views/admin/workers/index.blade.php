@extends('layouts.admin')
@section('title',$workSite->site_name.' Workers')
@section('page-title','Site Workers')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><a href="{{ route('admin.work-sites.show',$workSite) }}" class="text-decoration-none">← {{ $workSite->site_name }}</a><h2 class="mt-2 mb-1">Site Workers</h2><p class="muted mb-0">{{ $workers->total() }} workers assigned to this site.</p></div><div class="d-flex gap-2"><a class="btn btn-outline-light" href="{{ route('admin.work-sites.workers.bulk-create',$workSite) }}">+ Bulk Add</a><a class="btn btn-primary" href="{{ route('admin.work-sites.workers.create',$workSite) }}">+ Add Worker</a></div></div>
<form method="GET" class="row g-2 mb-3"><div class="col-md-7"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search worker, code, mobile or trade"></div><div class="col-md-3"><select class="form-select" name="role"><option value="">All worker roles</option>@foreach($roles as $value=>$label)<option value="{{ $value }}" @selected(request('role')===$value)>{{ $label }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-outline-light w-100">Filter</button></div></form>
<div class="card card-dark"><div class="table-responsive"><table class="table table-dark table-hover mb-0"><thead><tr><th>Worker</th><th>Role</th><th>Trade</th><th>Mobile</th><th>Wage</th><th>Status</th><th></th></tr></thead><tbody>@forelse($workers as $worker)<tr><td><strong>{{ $worker->name }}</strong><br><small class="muted">{{ $worker->worker_code }}</small></td><td>{{ $roles[$worker->role] ?? ucfirst($worker->role) }}</td><td>{{ $worker->trade }}</td><td>{{ $worker->mobile ?: '—' }}</td><td>₹{{ number_format($worker->daily_wage,2) }}</td><td>{{ $worker->active?'Active':'Inactive' }}</td><td class="text-end"><a class="btn btn-sm btn-outline-light" href="{{ route('admin.work-sites.workers.edit',[$workSite,$worker]) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.work-sites.workers.destroy',[$workSite,$worker]) }}" onsubmit="return confirm('Delete this worker?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center py-5">No workers added to this site.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $workers->links() }}</div>
@endsection
